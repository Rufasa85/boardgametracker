<form action="" method="POST" role="form">
    <legend>Form title</legend>

    <div class="form-group">
        <label for="">game</label>
        <input type="text" class="form-control" id="game" name ='game' placeholder="Input field">
    </div>

    <div class="form-group">
        <label for="">player 1</label>
        <select name = 'player1' id = 'player1'>
            <option value = false>none</option>
            <% players.forEach(function(player) { %>
                <option value = '<%= player.id %>'> <%= player.name %></option>
            <% }) %>
        </select>
    </div>

     <div class="form-group">
        <label for="">player 2</label>
        <select name = 'player2' id = 'player2'>
            <option value = false>none</option>
            <% players.forEach(function(player) { %>
                <option value = '<%= player.id %>'><%= player.name %></option>
            <% }) %>
        </select>
    </div>

    <div class="form-group">
        <label for="">player 3</label>
        <select name = 'player3' id = 'player3'>
            <option value = false>none</option>
            <% players.forEach(function(player) { %>
                <option value = '<%= player.id %>'><%= player.name %></option>
            <% }) %>
        </select>
    </div>

    <div class="form-group">
        <label for="">player 4</label>
        <select name = 'player4' id = 'player4'>
            <option value = false>none</option>
            <% players.forEach(function(player) { %>
                <option value = '<%= player.id %>'><%= player.name %></option>
            <% }) %>
        </select>
    </div>

    <div class="form-group">
        <label for="">player 5</label>
        <select name = 'player5' id = 'player5'>
            <option value = false>none</option>
            <% players.forEach(function(player) { %>
                <option value = '<%= player.id %>'><%= player.name %></option>
            <% }) %>
        </select>
    </div>


    <div class="form-group">
        <label for="">winner</label>
        <input type="text" class="form-control" id="winner" name = 'winner' placeholder="Input field">
    </div>



    <button type="submit" class="btn btn-primary">Submit</button>
</form>
